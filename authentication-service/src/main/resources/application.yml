spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://mysql:3306/authentication_api?allowPublicKeyRetrieval=true&useSSL=false
    username: developer
    password: junior087
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
rabbitmq:
  emailpendente:
    exchange: email-pendente.ex

springdoc:
  swagger-ui:
    path: /docs-auth.html
  api-docs:
    path: /docs-auth
  packagesToScan: com.ej.authentication.web.controllers
